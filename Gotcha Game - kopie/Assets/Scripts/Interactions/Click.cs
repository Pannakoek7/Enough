using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class Click : ParentInteraction
{
    /*myDelegate myEvent;

    protected override void Start()
    {
        base.Start();
        myEvent += objHandler.ChangeTask;
        //lambda to pass parameter
        myEvent += () => {th.ProgressText(mc.myText.Intro, mc.mcText);};
    }

    void OnMouseDown()
    {
        if(myEvent != null)
        {
            myEvent.Invoke();
        }
        
        myEvent = null;
    }*/

}
